我准备卖出${securityQuote.security.toString()}距离到期日${optionsChain.dte()}天的期权<#if summary??>，策略ID:${summary.strategy.strategyId}<#if summary.holdStockNum??>，当前持有股票数量：${summary.holdStockNum}<#if (summary.holdStockCost?? && summary.holdStockCost > 0)>，持有股票成本价：${summary.holdStockCost}</#if></#if></#if>当前股票价格是${securityPrice}<#if vixIndicator?? && vixIndicator.currentVix??>，当前VIX指数是${vixIndicator.currentVix.value}</#if>，当前日期是${currentDate}，接下来我将使用markdown格式给你提供一些信息，我需要你根据我提供的信息给我交易建议。
